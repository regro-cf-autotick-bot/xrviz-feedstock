{% set version = "0.1.3" %}
{% set name = "xrviz" %}
{% set sha256 = "8b2680bd335368b6319a004c084bccc8f31447b2e4a18064a9ed92ae0adbd3bc" %}

package:
   name:  {{ name }}
   version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: "{{ PYTHON }}  -m pip install . --no-deps --ignore-installed -vv"

requirements:
  host:
    - python >=3.6
    - pip
    - jinja2
  run:
    - python >=3.6
    - xarray
    - cartopy
    - panel >=0.5.1
    - holoviews ==1.12.1
    - hvplot
    - netcdf4
    - datashader
    - metpy

test:
  imports:
    - xrviz

about:
  home: https://github.com/intake/xrviz
  license: BSD-3-Clause
  license_file: LICENSE
  summary: "Interactive visualisation interface for Xarrays"

extra:
  recipe-maintainers:
    - martindurant
    - rsignell-usgs
    - hdsingh
